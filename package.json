{
  "name": "dragon-swim-team",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pretest": "node scripts/free-emulator-ports.mjs",
    "test": "firebase emulators:exec --only auth,firestore --project demo-dragon-swim \"playwright test\"",
    "test:ui": "node scripts/free-emulator-ports.mjs && firebase emulators:exec --only auth,firestore --project demo-dragon-swim \"playwright test --ui\""
  },
  "devDependencies": {
    "@faker-js/faker": "^10.4.0",
    "@playwright/test": "^1.60.0",
    "firebase-admin": "^13.10.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "firebase": "^12.13.0",
    "xlsx": "^0.18.5"
  }
}
